name: email-tests

on:
  pull_request:
  push:
    paths:
      - "src/lib/email/**"
      - "emails/**"
      - "src/app/api/**"
      - "supabase/migrations/**"
      - ".github/workflows/email-tests.yml"

jobs:
  unit:
    name: Email unit & security tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: |
          npm ci || npm install --no-audit --no-fund

      - name: Run email tests
        run: |
          node --experimental-strip-types --test \
            src/lib/email/validation.test.ts \
            src/lib/email/retry.test.ts \
            src/lib/email/ssrf.test.ts \
            src/lib/email/attachments.test.ts \
            src/lib/email/double-optin.test.ts

  typecheck:
    name: TypeScript typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: |
          npm ci || npm install --no-audit --no-fund

      - name: Typecheck
        run: npm run typecheck

  lint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: |
          npm ci || npm install --no-audit --no-fund

      - name: Lint
        run: npm run lint

  build:
    name: Next.js build
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || 'https://placeholder.supabase.co' }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY || 'placeholder' }}
      NEXT_PUBLIC_SITE_URL: "https://www.logicintelligencetechnologies.in"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: |
          npm ci || npm install --no-audit --no-fund

      - name: Build
        run: npm run build
