# Form Fixes — 2026-09-17

1. **Support tickets** — user_id NOT NULL blocked public inserts. Migration + API requester_* fields.
2. **Discovery** — silent failure; now surfaces setSubmitError + timeout.
3. **Free Demo** — require data.success before thank-you UI.
