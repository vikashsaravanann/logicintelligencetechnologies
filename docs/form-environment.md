# Form Environment Variables (names only)

Required: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, SMTP_*, LEAD_NOTIFICATION_EMAIL, CRON_SECRET, RESOURCE_ACCESS_SECRET.
Optional: EMAIL_DRY_RUN (must not be true in production).
Apply migration 20260917120000_support_tickets_public.sql for public support form.
