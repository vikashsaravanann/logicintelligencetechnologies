# Admin Navigation Audit

- `/admin` redirects to `/admin/command-center`
- `ADMIN_NAV_ITEMS` in `src/config/admin-nav.ts`
- Nav: Command Center, Leads, AI Leads, Bookings, Proposals, Support, Exit
- Nested pages use `AdminBackLink` → Admin Dashboard
